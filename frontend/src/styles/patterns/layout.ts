/** Standard page content wrapper */
export const pageWrapper = 'max-w-6xl mx-auto px-6 pt-25 pb-16';

/** Nav text links */
export const navLink =
  'text-sm text-fray-text-light hover:text-fray-text transition-colors duration-200';

/** Dropdown menu item */
export const menuItem =
  'block px-4 py-2 text-sm text-fray-text-light hover:text-fray-text hover:bg-fray-surface-alt transition-colors duration-150';

/** Hairline separator — use as a border-t on the element that needs the line above it */
export const divider = 'border-t border-fray-surface-alt';

/** Full-screen modal backdrop */
export const modalOverlay =
  'fixed inset-0 z-50 flex items-center justify-center bg-fray-overlay px-4';
