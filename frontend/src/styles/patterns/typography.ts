/** H1 — page-level title */
export const pageTitle = "text-3xl font-bold text-fray-text";

/** Subtitle under a page title */
export const pageSubtitle = "text-sm text-fray-text-light";

/** Section header label — small uppercase */
export const sectionLabel =
  "text-xs font-semibold uppercase tracking-widest text-fray-text-faint";

/** Article source/category tag — orange uppercase */
export const sourceTag =
  "text-xs font-semibold uppercase tracking-widest text-fray-primary";

/** Body copy */
export const bodyText = "text-sm text-fray-text-light leading-relaxed";

/** Timestamps, view counts, and other meta */
export const metaText = "text-xs text-fray-text-faint";

/** Inline link — orange, hover darker */
export const linkPrimary =
  "text-sm font-semibold text-fray-primary hover:text-fray-primary-hover transition-colors duration-200";
