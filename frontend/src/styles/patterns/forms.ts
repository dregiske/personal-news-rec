/** Full-width text/password/select input */
export const formInput =
  "bg-fray-input-bg text-fray-text text-sm px-4 py-2.5 rounded-xl outline-none " +
  "focus:ring-2 focus:ring-fray-primary/30 focus:bg-fray-surface " +
  "transition-all duration-200 placeholder:text-fray-muted disabled:opacity-50 w-full shadow-fray-sm";

/** Label above an input */
export const formLabel =
  "text-xs font-semibold uppercase tracking-widest text-fray-text-faint";

/** Inline validation error */
export const formError = "text-xs text-fray-danger";
