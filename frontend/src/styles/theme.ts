// Design tokens are now defined in styles/tokens.css (@theme) and consumed
// as Tailwind classes (e.g. text-fray-primary, bg-fray-surface).
// This file is kept for reference only — do not import from it.
//
// Token reference:
//   Colors   → src/styles/tokens.css
//   Patterns → src/styles/patterns/*  (re-exported via src/styles/index.ts)
