===== BACKEND =====

Tests
27 passing tests across test_routes.py and test_recommendation.py

---------- To Do ----------
- APScheduler

Issue                                       File            Fix
--------------------------------------------------------------------------------------------------------------



===== FRONTEND =====

---------- To Do ----------
- none

Issue                               		File                Fix
------------------------------------------------------------------------------------
No client-side input validation     		Login/Signup        Add basic checks (password length, email
                                    		pages               format) before the API call

No error boundary                   		App.tsx             Wrap Dashboard in a React ErrorBoundary
                                                    			so a crash doesn't blank the whole app




---------- Scalability Path ----------

Redis caching  → Cache hybrid_recommend_articles() results per user_id
               → Invalidate on new interaction (TTL ~5 min is fine)

Background     → Move /admin/ingest to a scheduled job (APScheduler or Celery)
ingestion        instead of an HTTP endpoint triggered manually

Model serving  → At scale, move ML inference out of the FastAPI process
               → Dedicated inference service (e.g. TorchServe or a simple FastAPI sidecar)


Phase 2: Scheduler (APScheduler)
Now that Postgres is stable, add the scheduler. It's purely Python — no new infra. Easy to test locally.

Phase 4: Redis caching
With Redis running in Docker, wire up fastapi-cache2 or redis-py to cache the heavy read endpoints (/feed/latest, /feed/topics/{topic}, /feed/for-you). These are your hottest paths and will get hammered in production.



====== ALEMBIC ======

alembic revision --autogenerate -m "describe the change"
# review the generated file in alembic/versions/
alembic upgrade head
That's it. Alembic diffs your models against the live database and generates the exact SQL needed.


===== DOCKER =====

Edit code locally
docker compose build app (rebuilds just the app container)
docker compose up -d (restarts with the new build)

Your deployment workflow going forward
Code change:


# local
git push

# on the droplet
cd ~/TheFray
git pull
docker compose build app
docker compose up -d

Schema change (new model/column):


# local
alembic revision --autogenerate -m "describe change"
git push

# on the droplet
git pull
docker compose build app
docker compose up -d  # alembic upgrade head runs automatically on startup