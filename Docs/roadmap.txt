===== BACKEND =====

Tests
27 passing tests across test_routes.py and test_recommendation.py


---------- To Do ----------

Database
Issue                                           File            Fix
--------------------------------------------------------------------------------------------------------------


Code Quality
Issue                                           File            Fix
--------------------------------------------------------------------------------------------------------------


Security
Issue                                           File            Fix
--------------------------------------------------------------------------------------------------------------


---------- Scalability Path ----------

PostgreSQL     → Drop-in swap via DATABASE_URL + pip install psycopg2-binary
               → Your config.py + SQLAlchemy setup already supports it

Redis caching  → Cache hybrid_recommend_articles() results per user_id
               → Invalidate on new interaction (TTL ~5 min is fine)

Background     → Move /admin/ingest to a scheduled job (APScheduler or Celery)
ingestion        instead of an HTTP endpoint triggered manually

Containerize   → Dockerfile + docker-compose with postgres + backend services
               → config.py + .env pattern is already 12-factor friendly

Model serving  → At scale, move ML inference out of the FastAPI process
               → Dedicated inference service (e.g. TorchServe or a simple FastAPI sidecar)



===== FRONTEND =====

---------- To Do ----------

Code Quality
Issue                               File                Fix
------------------------------------------------------------------------------------
No client-side input validation     Login/Signup        Add basic checks (password length, email
                                    pages               format) before the API call

No error boundary                   App.tsx             Wrap Dashboard in a React ErrorBoundary
                                                        so a crash doesn't blank the whole app

Architecture
Issue                               File                Fix
------------------------------------------------------------------------------------
Incomplete feature-folder pattern   features/ vs        Either move all features into features/
                                    pages/ + api/       (each owns components + API + state)
                                                        or drop features/ entirely — mixing both
                                                        adds confusion

No TypeScript strictness            tsconfig            @types/react already installed. Enable
                                                        strict mode in tsconfig.json


---------- Done ----------

Backend
✓ GET /api/v1/me endpoint           users.py            Restores user from cookie on page refresh
✓ Cookie-based auth                 auth.py             get_current_user reads from cookie OR
                                                        Authorization header (auto_error=False)

Frontend
✓ Token in localStorage removed     AuthContext.tsx     Relies on httpOnly cookie + withCredentials
✓ Session restore on refresh        AuthContext.tsx     fetchMe() on mount — falls back to null
✓ ProtectedRoute flash fix          ProtectedRoute.tsx  Returns null while session check is in flight
✓ Article.id type fixed             types.ts            string → number, matches backend int
✓ InteractionType includes view     types.ts            Added 'view' to match all backend types
✓ useFeed hook extracted            hooks/useFeed.ts    Data fetching isolated from Dashboard UI
✓ ArticleCard component             components/         Per-article rendering, interaction state,
                                    ArticleCard.tsx     view recording, Like/Dislike feedback
✓ Dashboard slimmed down            Dashboard.tsx       Pure layout — 20 lines, no fetch logic
✓ feed.ts + for-you.ts merged       api/feed.ts         One file, two named exports
✓ for-you.ts deleted                —                   Redundant file removed
✓ view interaction recorded         ArticleCard.tsx     Fires on "Read more" click (best-effort)
✓ Like/Dislike feedback             ArticleCard.tsx     Visual state + error message on failure
✓ API_BASE defined once             api/api.ts          All endpoints import and use API_BASE
✓ Shared styles system              styles/theme.ts     Design tokens: colors, spacing, font, radius
                                    styles/common.ts    Reusable style objects built from tokens
✓ PageLayout component              components/         Template wrapper: title, subtitle, children
                                    PageLayout.tsx
✓ NavBar + ArticleCard updated      components/         Pull from theme.ts and common.ts
✓ 404 Not Found page                pages/NotFound.tsx  Catch-all route path="*" in App.tsx
