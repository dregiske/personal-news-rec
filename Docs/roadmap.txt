===== BACKEND =====

Tests
27 passing tests across test_routes.py and test_recommendation.py


---------- To Do ----------

Database
Issue                                           File            Fix
--------------------------------------------------------------------------------------------------------------


Code Quality
Issue                                           File            Fix
--------------------------------------------------------------------------------------------------------------
Missing GET /api/v1/me endpoint                 users.py        Frontend needs this to restore user state
                                                                on page refresh from existing cookie.
                                                                Returns current user (id, email) or 401.


Security
Issue                                           File            Fix
--------------------------------------------------------------------------------------------------------------


---------- Scalability Path ----------

PostgreSQL     → Drop-in swap via DATABASE_URL + pip install psycopg2-binary
               → Your config.py + SQLAlchemy setup already supports it

Redis caching  → Cache hybrid_recommend_articles() results per user_id
               → Invalidate on new interaction (TTL ~5 min is fine)

Background     → Move /admin/ingest to a scheduled job (APScheduler or Celery)
ingestion        instead of an HTTP endpoint triggered manually

Containerize   → Dockerfile + docker-compose with postgres + backend services
               → config.py + .env pattern is already 12-factor friendly

Model serving  → At scale, move ML inference out of the FastAPI process
               → Dedicated inference service (e.g. TorchServe or a simple FastAPI sidecar)



===== FRONTEND =====

---------- To Do ----------

Critical (correctness + security)
Issue                               File                Fix
------------------------------------------------------------------------------------
Token in localStorage (XSS risk)    AuthContext.tsx     Backend already sets httpOnly cookie on login.
                                                        Drop localStorage.setItem/getItem, rely on
                                                        cookie + withCredentials: true (already set)

User lost on page refresh           AuthContext.tsx     On AuthProvider mount, call GET /api/v1/me
                                                        (needs backend endpoint) to restore user from
                                                        existing cookie — fall back to null if 401

Article.id typed as string          types.ts            Backend returns id as int. Change Article.id
                                                        to number — fixes silent type mismatch in
                                                        recordInteraction() calls

Code Quality
Issue                               File                Fix
------------------------------------------------------------------------------------
Dashboard does too much             Dashboard.tsx       Extract useFeed() custom hook for data
                                                        fetching/loading/error state. Extract
                                                        ArticleCard component for per-article UI

auth API not co-located with        features/auth/      Move login/signup API calls from
rest of API layer                   api.ts              features/auth/api.ts → src/api/auth.ts
                                                        to match feed.ts, stats.ts, interactions.ts

feed.ts and for-you.ts are          api/feed.ts         Merge into one file with two named exports:
two files for related endpoints     api/for-you.ts      fetchFeed() and fetchForYou()

No feedback on Like/Dislike         Dashboard.tsx       sendInteraction() fires and forgets. Add
                                                        optimistic UI, catch errors, show confirmation

view interaction never sent         Dashboard.tsx       Backend weights view: 1.0 in recommendations
                                                        but frontend never records it. Fire on article
                                                        click / link open

No client-side input validation     Login/Signup        Add basic checks (password length, email
                                    pages               format) before the API call

No error boundary                   App.tsx             Wrap Dashboard in a React ErrorBoundary
                                                        so a crash doesn't blank the whole app

Inline styles everywhere            All pages/          Move to CSS Modules (*.module.css) or
                                    components          centralize in main.css — enables media
                                                        queries and pseudo-selectors

Architecture
Issue                               File                Fix
------------------------------------------------------------------------------------
Incomplete feature-folder pattern   features/ vs        Either move all features into features/
                                    pages/ + api/       (each owns components + API + state)
                                                        or drop features/ entirely — mixing both
                                                        adds confusion

No TypeScript strictness            tsconfig            @types/react already installed. Enable
                                                        strict mode in tsconfig.json
